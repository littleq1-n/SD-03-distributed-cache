## 1. 项目骨架

- [x] 1.1 创建模块结构:`lru.py`、`protocol.py`、`server.py`、`hashring.py`、`client.py`、`replication.py`
- [x] 1.2 添加 `README.md`(运行方式、协议示例)与可选 `requirements.txt`(`pytest`)
- [x] 1.3 建立测试目录 `tests/`

## 2. LRU + TTL(单机存储层)

- [x] 2.1 实现双向链表节点与哈希表骨架(`dict[key] → node`)
- [x] 2.2 实现 `get`/`set`/`delete`,命中移到表头,O(1)
- [x] 2.3 实现 `maxsize` 容量上限与表尾淘汰
- [x] 2.4 实现 TTL:存绝对 deadline,读时惰性删除过期 key
- [x] 2.5 实现定期采样扫描清理过期冷 key
- [x] 2.6 单测:命中刷新、容量淘汰顺序、惰性删除、采样清理

## 3. 协议编解码(协议层)

- [x] 3.1 实现响应编码:`+OK`、`-ERR`、`$<len>` bulk、`$-1` 空值
- [x] 3.2 实现请求编码(客户端用)
- [x] 3.3 实现带读缓冲的解析器,按 `\r\n` 与 `$<len>` 分帧
- [x] 3.4 处理半包(数据不足时等待)与粘包(一次多条切分)
- [x] 3.5 单测:半包等待、粘包拆分、跨多次读取的 bulk 值、含空格/二进制值

## 4. asyncio TCP 服务器(网络层)

- [x] 4.1 实现 asyncio 服务器,每连接一协程,接受并发连接
- [x] 4.2 接入解析器与 LRU,分发 `SET`/`GET`/`DEL`/设 TTL 命令
- [x] 4.3 未知命令返回 `-ERR`;连接关闭释放资源
- [x] 4.4 确保写路径"应用本地 + 入复制队列"间无 `await`(临界区注释标注)
- [x] 4.5 可用 `nc`/`telnet` 手动验证;编写并发连接集成测试

## 5. 一致性哈希 + 客户端分片(分片层)

- [x] 5.1 实现哈希环 + 虚拟节点,key→顺时针首个虚拟节点→物理节点
- [x] 5.2 实现增删节点,验证仅约 1/N 的 key 改变归属
- [x] 5.3 实现客户端:本地维护环,算归属节点并直连发送命令
- [x] 5.4 单测:路由确定性、分布均匀性、增删节点影响范围、transient-miss 行为
- [x] 5.5 集成测试:多端口起多节点,验证 key 落点与 miss-回源语义

## 6. 主从复制(复制层)

- [x] 6.1 实现复制流:单 TCP 长连接 + 以 effect batch 为单位的单调递增 offset
- [x] 6.2 实现 effect batch 语义:写转发、淘汰/过期合成 `DEL` 并与触发写同属一个 batch、相对 TTL 改写为绝对时间
- [x] 6.3 区分 master `set`(触发淘汰)与 slave `apply_repl_set`(非淘汰),slave 以 batch 原子回放、不自主淘汰/过期删除
- [x] 6.4 实现"应答不等 ACK":本地应用 + effect 入队后即返回客户端;无 slave 连接时写仍成功
- [x] 6.5 实现重连一律全量同步:快照 + `snapshot_offset` + 期间 batch 缓冲 + 替换 store + 补发缓冲 batch
- [x] 6.6 实现手动故障切换(提升 slave 为 master)
- [x] 6.7 集成测试:offset 顺序一致、batch 原子回放、淘汰/过期一致、重连不丢快照期间的写

## 7. 端到端演示

- [x] 7.1 演示脚本:起分片 + 主从拓扑,跑一遍完整读写/复制/手动切换流程
